name: Build Wireless Backports Modules for OP11R

on:
  workflow_dispatch:
    inputs:
      manifest:
        description: "Manifest preset"
        required: true
        default: "OOS16"
        type: choice
        options:
          - OOS14
          - OOS15
          - OOS16
      backports_release:
        description: "Stable backports release"
        required: true
        default: "backports-6.1.97-1"
        type: choice
        options:
          - backports-6.1.97-1

jobs:
  build-wireless-modules:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      actions: read

    env:
      MODEL: OP11R
      SOC: waipio
      CONFIG: OP11R
      BRANCH: oneplus/sm8475
      MANIFEST_OOS14_MODULES: oneplus_11r_u.xml
      MANIFEST_OOS15_MODULES: oneplus_11r_v.xml
      MANIFEST_OOS16_MODULES: oneplus_11r_b.xml

    steps:
      - name: Validate target
        run: |
          set -e
          [ "$MODEL" = "OP11R" ] || exit 1
          [ "$SOC" = "waipio" ] || exit 1
          echo "✓ Target validation passed"

      - name: Cleanup runner
        run: |
          set -e
          sudo rm -rf /usr/share/dotnet /usr/share/swift /usr/local/.ghcup /usr/local/julia* \
            /usr/local/lib/android /usr/local/share/chromium /opt/microsoft /opt/google \
            /opt/az /usr/local/share/powershell /opt/hostedtoolcache 2>/dev/null || true
          docker system prune -af 2>/dev/null || true
          docker builder prune -af 2>/dev/null || true
          echo "Disk space after cleanup:"
          df -h

      - name: Install dependencies
        run: |
          set -e
          sudo apt-get update -qq
          sudo DEBIAN_FRONTEND=noninteractive apt-get install -y --no-install-recommends \
            git curl ca-certificates build-essential clang lld flex bison \
            libelf-dev libssl-dev libncurses-dev zlib1g-dev liblz4-tool \
            libxml2-utils rsync unzip dwarves file python3 ccache wget patch jq \
            coccinelle kmod bc perl xz-utils zip device-tree-compiler
          sudo apt-get clean
          echo "✓ Dependencies installed"

      - name: Set manifest
        run: |
          set -e
          case "${{ github.event.inputs.manifest }}" in
            OOS14) echo "MANIFEST=$MANIFEST_OOS14_MODULES" >> "$GITHUB_ENV" ;;
            OOS15) echo "MANIFEST=$MANIFEST_OOS15_MODULES" >> "$GITHUB_ENV" ;;
            OOS16) echo "MANIFEST=$MANIFEST_OOS16_MODULES" >> "$GITHUB_ENV" ;;
            *) echo "✗ Unknown manifest"; exit 1 ;;
          esac
          echo "✓ Manifest set to: $MANIFEST"

      - name: Install repo
        run: |
          set -e
          sudo curl -L https://storage.googleapis.com/git-repo-downloads/repo -o /usr/local/bin/repo
          sudo chmod +x /usr/local/bin/repo
          repo --version
          echo "✓ Repo tool installed"

      - name: Sync OP11R kernel source
        run: |
          set -e
          mkdir -p "$CONFIG"
          cd "$CONFIG"
          echo "Initializing repo..."
          repo init -u https://github.com/OnePlusOSS/kernel_manifest.git \
            -b "$BRANCH" -m "$MANIFEST" --repo-rev=v2.16 --depth=1 --no-clone-bundle --no-tags
          
          echo "Syncing kernel sources (3 retries)..."
          for i in 1 2 3; do
            echo "Attempt $i..."
            if repo sync -c --no-clone-bundle --no-tags --optimized-fetch -j$(nproc) --fail-fast; then
              echo "✓ Sync successful"
              break
            fi
            if [ $i -lt 3 ]; then
              echo "⟳ Sync failed, retrying in 20 seconds..."
              sleep 20
            else
              echo "✗ Sync failed after 3 attempts"
              exit 1
            fi
          done

      - name: Detect kernel version and prepare tree
        run: |
          set -e
          MSM="$GITHUB_WORKSPACE/$CONFIG/kernel_platform/msm-kernel"
          cd "$MSM"
          
          echo "Detecting kernel version..."
          VERSION=$(grep '^VERSION *=' Makefile | awk '{print $3}')
          PATCHLEVEL=$(grep '^PATCHLEVEL *=' Makefile | awk '{print $3}')
          SUBLEVEL=$(grep '^SUBLEVEL *=' Makefile | awk '{print $3}')
          KERNEL_VER="${VERSION}.${PATCHLEVEL}.${SUBLEVEL}"
          echo "KERNEL_VER=$KERNEL_VER" >> "$GITHUB_ENV"
          echo "✓ Kernel version: $KERNEL_VER"
          
          echo "Cleaning kernel tree..."
          make ARCH=arm64 mrproper
          
          mkdir -p out
          echo "Merging kernel configs..."
          ./scripts/kconfig/merge_config.sh -O out -m \
            arch/arm64/configs/gki_defconfig \
            arch/arm64/configs/vendor/${SOC}_GKI.config || {
            echo "⟳ Config merge warning, continuing..."
          }
          
          cp out/.config arch/arm64/configs/gki_defconfig
          make O=out ARCH=arm64 olddefconfig </dev/null

      - name: Detect clang
        run: |
          set -e
          KP="$GITHUB_WORKSPACE/$CONFIG/kernel_platform"
          FOUND=false
          
          for base in "$KP/prebuilts" "$KP/prebuilts-master"; do
            if [ ! -d "$base/clang/host/linux-x86" ]; then
              continue
            fi
            latest=$(ls -d "$base"/clang/host/linux-x86/clang-r*/ 2>/dev/null | sort -V | tail -n1 || true)
            if [ -n "$latest" ] && [ -x "$latest/bin/clang" ]; then
              echo "CLANG_BIN=$latest/bin" >> "$GITHUB_ENV"
              FOUND=true
              echo "✓ Found prebuilt clang at: $latest/bin"
              break
            fi
          done
          
          if ! $FOUND; then
            SYSTEM_CLANG=$(dirname "$(command -v clang)")
            echo "CLANG_BIN=$SYSTEM_CLANG" >> "$GITHUB_ENV"
            echo "✓ Using system clang at: $SYSTEM_CLANG"
          fi

      - name: Kernel prepare
        run: |
          set -e
          MSM="$GITHUB_WORKSPACE/$CONFIG/kernel_platform/msm-kernel"
          OUT="$MSM/out"
          
          export PATH="${CLANG_BIN}:$PATH:/usr/lib/ccache"
          export HOSTCC=clang
          export HOSTCXX=clang++
          
          declare -a MAKE_FLAGS=(
            LLVM=1
            LLVM_IAS=1
            ARCH=arm64
            SUBARCH=arm64
            CLANG_TRIPLE=aarch64-linux-gnu-
            CROSS_COMPILE=aarch64-linux-gnu-
            CROSS_COMPILE_COMPAT=arm-linux-gnueabi-
            LD=ld.lld
            HOSTLD=ld.lld
            AR=llvm-ar
            NM=llvm-nm
            OBJCOPY=llvm-objcopy
            OBJDUMP=llvm-objdump
            DEPMOD=depmod
            DTC=dtc
            STRIP=llvm-strip
            CC=clang
          )
          
          cd "$MSM"
          echo "Step 1: Generate gki_defconfig..."
          make "${MAKE_FLAGS[@]}" O="$OUT" gki_defconfig
          
          echo "Step 2: Configure WiFi modules to modular..."
          if [ -x ./scripts/config ]; then
            ./scripts/config --file "$OUT/.config" --set-val CONFIG_CFG80211 m || true
            ./scripts/config --file "$OUT/.config" --set-val CONFIG_MAC80211 m || true
          fi
          
          echo "Step 3: Prepare kernel headers and modules_prepare..."
          make "${MAKE_FLAGS[@]}" O="$OUT" olddefconfig </dev/null
          make "${MAKE_FLAGS[@]}" O="$OUT" prepare modules_prepare 2>&1 | tail -20
          
          echo "Step 4: Disable incompatible kernel features..."
          if [ -x ./scripts/config ]; then
            ./scripts/config --file "$OUT/.config" --disable CONFIG_CFI || true
            ./scripts/config --file "$OUT/.config" --disable CONFIG_CFI_CLANG || true
            ./scripts/config --file "$OUT/.config" --disable CONFIG_LTO_CLANG || true
            ./scripts/config --file "$OUT/.config" --disable CONFIG_LTO_CLANG_THIN || true
            ./scripts/config --file "$OUT/.config" --disable CONFIG_SHADOW_CALL_STACK || true
            ./scripts/config --file "$OUT/.config" --disable CONFIG_KASAN || true
            ./scripts/config --file "$OUT/.config" --disable CONFIG_KASAN_SW_TAGS || true
            ./scripts/config --file "$OUT/.config" --disable CONFIG_KASAN_HW_TAGS || true
            ./scripts/config --file "$OUT/.config" --disable CONFIG_UBSAN || true
            ./scripts/config --file "$OUT/.config" --disable CONFIG_UBSAN_TRAP || true
            ./scripts/config --file "$OUT/.config" --disable CONFIG_UBSAN_BOUNDS || true
          fi
          
          echo "Step 5: Final olddefconfig and prepare..."
          make "${MAKE_FLAGS[@]}" O="$OUT" olddefconfig </dev/null
          make "${MAKE_FLAGS[@]}" O="$OUT" prepare modules_prepare 2>&1 | tail -20
          
          echo "✓ Kernel preparation complete"
          echo "Kernel build output location: $OUT"
          ls -la "$OUT" | head -20

      - name: Download backports
        run: |
          set -e
          RELEASE="${{ github.event.inputs.backports_release }}"
          echo "Downloading backports: $RELEASE"
          
          wget -v -O backports.tar.xz \
            "https://cdn.kernel.org/pub/linux/kernel/projects/backports/stable/v6.1.97/backports-6.1.97-1.tar.xz"
          
          echo "✓ Downloaded, extracting..."
          tar -xf backports.tar.xz
          mv "$RELEASE" backports-stable
          
          echo "✓ Backports extracted"
          ls -la backports-stable/ | head -10

      - name: Patch compat for CFI
        run: |
          set -e
          cd backports-stable
          
          FILE="backport-include/linux/module.h"
          if [ -f "$FILE" ]; then
            echo "Patching CFI compatibility in $FILE..."
            sed -i 's/__CFI_ADDRESSABLE(init_module, __initdata)/__CFI_ADDRESSABLE(init_module)/g' "$FILE"
            sed -i 's/__CFI_ADDRESSABLE(cleanup_module, __exitdata)/__CFI_ADDRESSABLE(cleanup_module)/g' "$FILE"
            echo "✓ CFI patch applied"
          else
            echo "⟳ File not found, skipping CFI patch"
          fi

      - name: Configure backports (Mediatek + Realtek only)
        run: |
          set -e
          KERNEL_BUILD="$GITHUB_WORKSPACE/$CONFIG/kernel_platform/msm-kernel/out"
          
          export PATH="${CLANG_BIN}:$PATH:/usr/lib/ccache"
          
          declare -a MAKE_FLAGS=(
            LLVM=1
            LLVM_IAS=1
            ARCH=arm64
            SUBARCH=arm64
            CLANG_TRIPLE=aarch64-linux-gnu-
            CROSS_COMPILE=aarch64-linux-gnu-
            CROSS_COMPILE_COMPAT=arm-linux-gnueabi-
            LD=ld.lld
            HOSTLD=ld.lld
            AR=llvm-ar
            NM=llvm-nm
            OBJCOPY=llvm-objcopy
            OBJDUMP=llvm-objdump
            DEPMOD=depmod
            DTC=dtc
            STRIP=llvm-strip
            HOSTCC=clang
            HOSTCXX=clang++
            CC=clang
          )
          
          cd backports-stable
          
          echo "Creating minimal backports config..."
          make "${MAKE_FLAGS[@]}" \
            KLIB="$KERNEL_BUILD" \
            KLIB_BUILD="$KERNEL_BUILD" \
            defconfig 2>&1 | tee backports-config.log
          
          echo ""
          echo "Configuring for Mediatek and Realtek USB drivers..."
          if [ -x ./scripts/config ]; then
            # Enable core wireless
            ./scripts/config --file .config --set-val CONFIG_CFG80211 m || true
            ./scripts/config --file .config --set-val CONFIG_MAC80211 m || true
            
            # Enable Mediatek drivers
            ./scripts/config --file .config --set-val CONFIG_MT7921_COMMON m || true
            ./scripts/config --file .config --set-val CONFIG_MT7921U m || true
            ./scripts/config --file .config --set-val CONFIG_MT76_USB m || true
            ./scripts/config --file .config --set-val CONFIG_MT76 m || true
            
            # Enable Realtek drivers
            ./scripts/config --file .config --set-val CONFIG_RTL8XXXU m || true
            ./scripts/config --file .config --set-val CONFIG_RTL8188EU m || true
            ./scripts/config --file .config --set-val CONFIG_RTL8192EU m || true
            ./scripts/config --file .config --set-val CONFIG_RTL8723BU m || true
            ./scripts/config --file .config --set-val CONFIG_RTW88 m || true
            ./scripts/config --file .config --set-val CONFIG_RTW88_USB m || true
            
            # DISABLE Intel drivers to avoid build issues
            ./scripts/config --file .config --disable CONFIG_IWLWIFI || true
            ./scripts/config --file .config --disable CONFIG_IWLMVM || true
            ./scripts/config --file .config --disable CONFIG_IWLDVM || true
            ./scripts/config --file .config --disable CONFIG_IWL7000 || true
            
            # Disable other problematic subsystems
            ./scripts/config --file .config --disable CONFIG_STAGING || true
          fi
          
          echo ""
          echo "Running olddefconfig to validate..."
          make "${MAKE_FLAGS[@]}" \
            KLIB="$KERNEL_BUILD" \
            KLIB_BUILD="$KERNEL_BUILD" \
            olddefconfig 2>&1 | tail -10
          
          echo ""
          echo "✓ Backports configuration complete"
          echo "Generated .config:"
          head -50 .config

      - name: Clean backports build directory
        run: |
          cd backports-stable
          echo "Cleaning previous build artifacts..."
          make mrproper || true
          rm -f Module.symvers modules.order *.order
          find . -name "modules.order" -delete
          find . -name ".tmp_versions" -type d -exec rm -rf {} + 2>/dev/null || true
          echo "✓ Build directory cleaned"

      - name: Build backports modules
        run: |
          set -e
          KERNEL_BUILD="$GITHUB_WORKSPACE/$CONFIG/kernel_platform/msm-kernel/out"
          
          export PATH="${CLANG_BIN}:$PATH:/usr/lib/ccache"
          export CCACHE_DIR="$GITHUB_WORKSPACE/.ccache"
          mkdir -p "$CCACHE_DIR"
          
          declare -a MAKE_FLAGS=(
            LLVM=1
            LLVM_IAS=1
            ARCH=arm64
            SUBARCH=arm64
            CLANG_TRIPLE=aarch64-linux-gnu-
            CROSS_COMPILE=aarch64-linux-gnu-
            CROSS_COMPILE_COMPAT=arm-linux-gnueabi-
            LD=ld.lld
            HOSTLD=ld.lld
            AR=llvm-ar
            NM=llvm-nm
            OBJCOPY=llvm-objcopy
            OBJDUMP=llvm-objdump
            DEPMOD=depmod
            DTC=dtc
            STRIP=llvm-strip
            HOSTCC=clang
            HOSTCXX=clang++
            CC=clang
          )
          
          cd backports-stable
          
          echo "Building backports modules with $(nproc) threads..."
          echo "Building: Mediatek MT7921/MT76 and Realtek RTL8XXXU drivers"
          
          # Reduce parallelism to avoid modules.order conflicts
          make -j4 \
            "${MAKE_FLAGS[@]}" \
            KLIB="$KERNEL_BUILD" \
            KLIB_BUILD="$KERNEL_BUILD" \
            V=1 \
            2>&1 | tee "$GITHUB_WORKSPACE/build.log"
          
          BUILD_EXIT=$?
          
          if [ $BUILD_EXIT -ne 0 ]; then
            echo "✗ Build failed with exit code $BUILD_EXIT"
            echo ""
            echo "Last 100 lines of build output:"
            tail -100 "$GITHUB_WORKSPACE/build.log"
            exit 1
          fi
          
          echo ""
          echo "✓ Build completed successfully"

      - name: Verify .ko files generated
        run: |
          set -e
          cd backports-stable
          
          echo "=== Searching for .ko files ==="
          KO_FILES=$(find . -name "*.ko" -type f 2>/dev/null)
          KO_COUNT=$(echo "$KO_FILES" | grep -c '\.ko$' || true)
          
          echo "Total .ko files found: $KO_COUNT"
          
          if [ "$KO_COUNT" -eq 0 ]; then
            echo "✗ CRITICAL: No .ko files generated!"
            echo ""
            echo "Directory structure:"
            find . -type d -name "wireless" -o -name "drivers" | head -20
            echo ""
            echo "Last 150 lines of build.log:"
            tail -150 "$GITHUB_WORKSPACE/build.log"
            exit 1
          fi
          
          echo ""
          echo "✓ .ko files generated:"
          echo "$KO_FILES" | sort
          
          echo ""
          echo "Module details:"
          for ko in $(echo "$KO_FILES" | sort); do
            echo "  $(du -h "$ko" | cut -f1)  $ko"
          done

      - name: Extract and organize modules
        run: |
          set -e
          cd backports-stable
          
          echo "Creating organized modules output directory..."
          mkdir -p ../modules-out
          
          echo "Copying .ko files..."
          find . -name "*.ko" -type f -exec cp -v {} ../modules-out/ \;
          
          echo ""
          echo "=== Files in modules-out ==="
          ls -lh ../modules-out/
          
          echo ""
          echo "Creating archive..."
          cd ../modules-out
          ARCHIVE_NAME="wireless-modules-op11r-mediatek-realtek-${GITHUB_RUN_NUMBER}.zip"
          zip -r "$GITHUB_WORKSPACE/$ARCHIVE_NAME" *.ko
          
          echo ""
          echo "✓ Archive created: $ARCHIVE_NAME"
          ls -lh "$GITHUB_WORKSPACE/$ARCHIVE_NAME"

      - name: Collect and verify artifacts
        if: always()
        run: |
          set -e
          mkdir -p artifacts
          
          echo "Collecting artifacts..."
          
          # Find the latest archive
          if ls "$GITHUB_WORKSPACE"/wireless-modules-op11r-mediatek-realtek-*.zip 1> /dev/null 2>&1; then
            cp "$GITHUB_WORKSPACE"/wireless-modules-op11r-mediatek-realtek-*.zip artifacts/ || true
            echo "✓ Wireless modules archive copied"
          fi
          
          # Copy logs
          if [ -f "$GITHUB_WORKSPACE/build.log" ]; then
            cp "$GITHUB_WORKSPACE/build.log" artifacts/build-full.log || true
            echo "✓ Build log copied"
          fi
          
          # Copy configs
          if [ -f backports-stable/.config ]; then
            cp backports-stable/.config artifacts/backports.config || true
            echo "✓ Backports config copied"
          fi
          
          if [ -f backports-stable/backports-config.log ]; then
            cp backports-stable/backports-config.log artifacts/ || true
            echo "✓ Config generation log copied"
          fi
          
          # Create summary
          cat > artifacts/BUILD_SUMMARY.txt <<EOF
          Build Summary
          =============
          Date: $(date)
          Kernel Version: ${KERNEL_VER:-unknown}
          Manifest: ${MANIFEST:-unknown}
          Backports Release: ${{ github.event.inputs.backports_release }}
          SOC: ${SOC}
          Model: ${MODEL}
          Build Type: Mediatek + Realtek USB WiFi drivers
          
          Drivers Included:
          - Mediatek MT7921/MT76 USB
          - Realtek RTL8XXXU (8188EU, 8192EU, 8723BU variants)
          - cfg80211/mac80211 core modules
          
          Intel drivers: DISABLED (to avoid build conflicts)
          
          Build Output: See artifacts/
          EOF
          
          echo ""
          echo "=== Artifacts collected ==="
          ls -lhR artifacts/

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: op11r-mediatek-realtek-modules-${{ github.run_number }}
          path: artifacts/
          retention-days: 30
          if-no-files-found: error

      - name: Create release notes
        if: success()
        run: |
          cat > RELEASE_NOTES.md <<EOF
          # OP11R Wireless USB Drivers - Mediatek + Realtek Build ${{ github.run_number }}
          
          ## Build Information
          - **Date**: $(date)
          - **Kernel Version**: ${KERNEL_VER}
          - **Manifest**: ${MANIFEST}
          - **Backports Release**: ${{ github.event.inputs.backports_release }}
          - **Build Status**: ✅ SUCCESS
          - **Purpose**: NetHunter WiFi USB adapters
          
          ## Supported Drivers
          ### Mediatek
          - MT7921 (MT7921U)
          - MT76 USB variants
          
          ### Realtek  
          - RTL8188EU
          - RTL8192EU
          - RTL8723BU
          - RTL8XXXU family
          - RTW88 USB
          
          ## Artifacts
          - **wireless-modules-op11r-mediatek-realtek-${{ github.run_number }}.zip** - All .ko modules
          - **build-full.log** - Complete build output
          - **backports.config** - Kernel configuration used
          - **BUILD_SUMMARY.txt** - Summary information
          
          ## Installation for NetHunter
          1. Extract the zip file on your OP11R
          2. Copy .ko files to `/system/lib/modules/` or appropriate module directory
          3. Run: \`insmod <module>.ko\` for each driver
          4. Or use: \`modprobe <module>\` if depmod is configured
          5. Verify with: \`lsmod | grep mt7921\` or \`lsmod | grep rtl\`
          
          ## Build Logs
          See artifacts for complete build logs and configuration files.
          EOF
          cat RELEASE_NOTES.md
